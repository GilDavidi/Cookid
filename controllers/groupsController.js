const User = require('../mongoDB/models/users');
let Groups =[];

module.exports = {
    createGroups: (req, res) => {
        req.send("Create Groups was succeed");
    },
    saveGroups: (req, res) => {

    }
}
